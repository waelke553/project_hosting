apiVersion: v1
kind: Service
metadata:
  name: {{ phpmyadmin_service_name }}
spec:
  selector:
    app: phpmyadmin
  ports:
  - protocol: TCP
    port: {{ phpmyadmin_outside_port }}
    targetPort: 80