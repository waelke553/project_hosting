apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ database_persistent_volume_name }}
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    server: {{ nfs_ip_address }}
    path: {{ nfs_file_path ~ new_namespace }}/db
